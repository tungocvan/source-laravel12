# Project Bootstrap

Canonical source: `.codex/bootstrap/PROJECT_BOOTSTRAP.md`.

This mirror exists because some rebuild workflows refer to `docs/PROJECT_BOOTSTRAP.md`. The canonical workspace file records the project-specific Laravel 12 stack, `composer.json` analysis, module provider behavior, PSR-4 autoloading, module namespace strategy, shared services, shared components, route registration, storage, assets, queue/cache strategy, and roadmap alignment.

Before writing code, read the canonical file and use it as the repository-specific source of truth.
