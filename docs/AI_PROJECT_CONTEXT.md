# AI Project Context

Canonical source: `.codex/bootstrap/AI_PROJECT_CONTEXT.md`.

This mirror exists because some rebuild workflows refer to `docs/AI_PROJECT_CONTEXT.md`. The canonical workspace file summarizes the project stack, packages, coding style, architecture decisions, reusable components, reusable traits, reusable services, and AI working rules for this codebase.

Before writing code, read the canonical file and combine it with `ROADMAP.md` and any target module docs.
