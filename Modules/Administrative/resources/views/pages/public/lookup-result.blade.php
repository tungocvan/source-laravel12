@extends('Administrative::layouts.public')
@section('title', 'Kết quả tra cứu '.$submission->submission_code)
@section('robots', 'noindex,nofollow,noarchive')
@section('content')
@php
    $status = $submission->status->value;
    $statusLabels = ['pending' => 'Chờ duyệt', 'approved' => 'Đã duyệt', 'rejected' => 'Bị từ chối', 'need_supplement' => 'Yêu cầu bổ sung'];
    $statusClasses = ['pending' => 'bg-amber-50 text-amber-700 ring-amber-600/20', 'approved' => 'bg-green-50 text-green-700 ring-green-600/20', 'rejected' => 'bg-red-50 text-red-700 ring-red-600/20', 'need_supplement' => 'bg-orange-50 text-orange-700 ring-orange-600/20'];
@endphp
<div class="mx-auto max-w-4xl px-4 py-10 sm:px-6"><div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h1 class="text-2xl font-bold">Kết quả tra cứu</h1><p class="mt-1 text-sm text-slate-500">Phiên truy cập tự hết hạn sau 15 phút.</p></div><a href="{{ route('administrative.lookup.index') }}" class="text-sm font-semibold text-indigo-600">Tra cứu hồ sơ khác</a></div>
<div class="mt-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><div class="text-sm font-semibold text-indigo-600">{{ $submission->submission_code }}</div><h2 class="mt-1 text-xl font-bold">{{ $submission->procedure->name }}</h2></div><span class="inline-flex w-fit rounded-full px-3 py-1 text-xs font-medium ring-1 ring-inset {{ $statusClasses[$status] ?? 'bg-slate-100 text-slate-700' }}">{{ $statusLabels[$status] ?? $status }}</span></div>
<dl class="mt-6 grid gap-4 border-t border-slate-200 pt-5 sm:grid-cols-2"><div><dt class="text-xs uppercase text-slate-500">Người nộp</dt><dd class="mt-1 font-medium">{{ $submission->applicant_name }}</dd></div><div><dt class="text-xs uppercase text-slate-500">Học sinh</dt><dd class="mt-1 font-medium">{{ $submission->student_name }}</dd></div><div><dt class="text-xs uppercase text-slate-500">Ngày nộp</dt><dd class="mt-1 font-medium">{{ $submission->submitted_at->format('d/m/Y H:i') }}</dd></div><div><dt class="text-xs uppercase text-slate-500">Ngày xử lý</dt><dd class="mt-1 font-medium">{{ $submission->processed_at?->format('d/m/Y H:i') ?? '-' }}</dd></div></dl>
@if($submission->response)<div class="mt-6 rounded-xl border border-indigo-200 bg-indigo-50 p-4"><h3 class="text-sm font-semibold text-indigo-900">Phản hồi</h3><p class="mt-1 whitespace-pre-line text-sm text-indigo-800">{{ $submission->response }}</p></div>@endif
@if($status === 'rejected')<div class="mt-6 rounded-xl border border-red-200 bg-red-50 p-4"><h3 class="text-sm font-semibold text-red-900">Lý do từ chối</h3><p class="mt-1 whitespace-pre-line text-sm text-red-800">{{ $submission->rejection_reason ?: 'Vui lòng liên hệ nhà trường để biết thêm chi tiết.' }}</p></div>@endif
@if(session('success'))<div class="mt-6 rounded-xl border border-green-200 bg-green-50 p-4 text-sm text-green-700">{{ session('success') }}</div>@endif
@if($status === 'need_supplement')@livewire('administrative.public.supplement-form', ['accessToken' => $accessToken], key('supplement-'.$submission->id.'-'.$submission->version))@endif
<section class="mt-6"><h3 class="font-semibold">File kết quả</h3>@if($submission->files->isEmpty())<div class="mt-3 rounded-xl border border-dashed border-slate-300 p-5 text-sm text-slate-500">Chưa có file kết quả.</div>@else<ul class="mt-3 divide-y divide-slate-200 rounded-xl border border-slate-200">@foreach($submission->files as $file)<li class="flex flex-col gap-2 p-4 sm:flex-row sm:items-center sm:justify-between"><div><div class="text-sm font-medium">{{ $file->original_name }}</div><div class="text-xs text-slate-500">{{ number_format($file->size / 1024, 0) }} KB · {{ $file->created_at->format('d/m/Y H:i') }}</div></div><a href="{{ route('administrative.lookup.files.download', [$accessToken, $file->id]) }}" class="text-sm font-semibold text-indigo-600">Tải xuống</a></li>@endforeach</ul>@endif</section>
</div></div>
@endsection
