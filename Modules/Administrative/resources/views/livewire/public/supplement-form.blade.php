<form wire:submit="resubmit" class="mt-6 rounded-2xl border border-amber-300 bg-amber-50 p-5">
    <h3 class="font-semibold text-amber-900">Bổ sung hồ sơ</h3>
    <p class="mt-2 whitespace-pre-line text-sm text-amber-800">{{ $submission->supplement_reason }}</p>
    <div class="mt-4 rounded-xl bg-white p-4"><label class="text-sm font-medium text-gray-700">Chọn file bổ sung</label><input wire:model="files" type="file" multiple class="mt-2 w-full rounded-xl border border-gray-300 px-4 py-3 text-sm"><p class="mt-2 text-xs text-gray-500">Tối đa {{ $submission->procedure->max_files }} file, {{ number_format($submission->procedure->max_file_size_kb / 1024, 0) }} MB/file.</p>@error('files')<p class="mt-1 text-sm text-red-600">{{ $message }}</p>@enderror @error('files.*')<p class="mt-1 text-sm text-red-600">{{ $message }}</p>@enderror<div wire:loading wire:target="files" class="mt-2 text-sm text-indigo-600">Đang tải file...</div>@if($files)<ul class="mt-2 text-sm text-gray-600">@foreach($files as $file)<li>{{ $file->getClientOriginalName() }}</li>@endforeach</ul>@endif</div>
    <label class="mt-4 flex items-start gap-3"><input wire:model="confirmation" type="checkbox" class="mt-1"><span class="text-sm text-amber-900">Tôi xác nhận đã bổ sung file theo yêu cầu.</span></label>@error('confirmation')<p class="mt-1 text-sm text-red-600">{{ $message }}</p>@enderror
    <button type="submit" wire:loading.attr="disabled" class="mt-4 inline-flex rounded-xl bg-amber-600 px-5 py-3 text-sm font-semibold text-white disabled:opacity-60"><span wire:loading.remove wire:target="resubmit">Gửi lại hồ sơ</span><span wire:loading wire:target="resubmit">Đang gửi...</span></button>
</form>
